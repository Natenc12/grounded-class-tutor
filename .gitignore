# --- Secrets / config ---
.env
.env.*
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / React (arrives in Slice 4) ---
node_modules/
dist/
build/
*.local

# --- Claude Code ---
# Ignore local/personal Claude config, but SHARE project skills + shared settings.
.claude/*
!.claude/skills/
!.claude/settings.json
.claude/settings.local.json

# --- Dogfood corpus (real course materials — never committed, ADR 0021 / C5) ---
# The eval bench references these by filename; the files themselves stay local.
# A committed manifest (data/dogfood/*/manifest.md) records what the questions point at.
data/dogfood/**
!data/dogfood/**/
!data/dogfood/**/manifest.md

# --- Personal understanding log ---
# Nate's own comprehension notes + glossary, in his words — not project truth.
# Personal for now; drop this line if it ever becomes a shared team artifact.
understanding/

# --- OS / editor noise ---
.DS_Store
.idea/
.vscode/
