guard    src/gct/jobs/worker.py:227 (added)
covers   src/gct/jobs/worker.py:227
mutant   `job_id = ...` dropped — the builder's flagged likely-equivalent mutant

```diff
-                  where job_id = %(job_id)s::uuid
-                    and state = 'processing'
+                  where state = 'processing'
```

result   GREEN — HOLE
         pytest tail: 546 passed, 1 deselected in 9.97s
         evidence/issue_86_zombie_bury_repro.py: exit=0  job 01926fff-da7b-461a-b09b-1dd5f633874d: lease lost before it could be failed - another worker owns it now
         failed: (none — no test distinguishes this mutant)

---

## SETTLED BY DIFFERENTIAL SEARCH — not a hole, not equivalent either

`differential.py` as harvested reimplements #66's pure string functions side by side; the
predicate here is SQL over the `jobs` table, so the same search was run against the real server
(`differential86.py`, kept beside this ledger).

**Part A — exhaustive over the state space.** Holder token (a live uuid, or NULL for a row
claimed before migrations/0002) x J's own row (absent, or 4 states x 3 tokens) x an optional
second row K (same domain) = **338 configurations, all evaluated**. 12 distinguish, and every
one of them is the SAME shape:

    held=T   J = anything (including absent)   K = ('processing', T)
    -> orig = False, mut = True

i.e. the mutant differs **iff some OTHER job row is `processing` carrying the token we hold.**
So the mutant is NOT equivalent: a distinguishing input exists, and nothing in the schema
forbids it (`grep -rniE 'unique.*lease_token' migrations/` -> no match).

**Part B — reachability.** `jobs.lease_token` is written by exactly two statements in the repo:
`queue.py:228` `lease_token = gen_random_uuid()` (claim, one row) and `queue.py:478`
`lease_token = null` (reclaim_expired). A NULL token distinguishes nothing (`= null` is never
true on either side). So the distinguishing state needs two live claims to draw the same v4
uuid.

**Verdict: no test is missing.** A test would have to hand-forge a row pair no production
statement can produce, and would pin `_settle`'s guard shape a second time. The conjunct stays
because it makes the guard `_settle`'s two conditions VERBATIM, which is the property
`_bury`'s docstring is built on — it is redundant today and load-bearing the moment anything
else writes that column.

The builder's D1 flagged this as "likely equivalent, a differential search is the honest way to
close it". The search says: **distinguishable in principle, unreachable in practice** — a
stronger and more accurate statement than the equivalence it guessed at.
